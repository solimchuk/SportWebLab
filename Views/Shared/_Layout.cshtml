<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SportClub</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/WebApplication1.styles.css" asp-append-version="true" />
</head>
<body class="d-flex flex-column min-vh-100">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand d-flex align-items-center" asp-area="" asp-controller="Home" asp-action="Index">
                    <span class="badge bg-warning text-dark me-2">🏀</span>
                    <span>SportClub</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="mainNav">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link" asp-controller="Teams" asp-action="Index">Teams</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Players" asp-action="Index">Players</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Sports" asp-action="Index">Sports</a></li>
                    </ul>

                    <!-- Авторизаційні кнопки замість пошуку -->
                    <div class="d-flex align-items-center ms-auto gap-2">
                        @if (User.Identity?.IsAuthenticated ?? false)
                        {
                            <span class="text-light me-2">Привіт, @User.Identity.Name</span>
                            <form asp-area="Identity" asp-page="/Account/Logout" method="post" class="d-inline">
                                <button type="submit" class="btn btn-brown btn-sm">Вихід</button>
                            </form>
                        }
                        else
                        {
                            <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-brown btn-sm">Реєстрація</a>
                            <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-brown btn-sm">Увійти</a>
                        }
                    </div>
                </div>
            </div>
        </nav>

        <div class="hero bg-gradient text-white py-5">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h1 class="display-6 fw-bold">SportClub — світ командного спорту</h1>
                        <p class="lead">Створюй команди, додавай гравців та слідкуй за статистикою. Баскетбол, волейбол, футбол — все в одному місці.</p>
                        <a class="btn btn-warning btn-lg me-2" asp-controller="Teams" asp-action="Create">Додати команду</a>
                        <a class="btn btn-outline-light btn-lg" asp-controller="Players" asp-action="Create">Додати гравця</a>
                    </div>
                    <div class="col-md-4 text-center">
                        <img src="~/images/basketball.png" alt="basketball" class="img-fluid hero-img" style="max-height:140px;" />
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- main як flex-fill, всередині контейнер -->
    <main class="flex-fill">
        <div class="container my-4">
            @RenderBody()
        </div>
    </main>

    <footer class="mt-auto bg-dark text-light py-4">
        <div class="container d-flex justify-content-between align-items-center">
            <div>
                &copy; @DateTime.Now.Year SportClub
            </div>
            <div class="text-end">
                <small>Follow us:
                    <a class="text-warning text-decoration-none ms-2" href="#" aria-label="instagram">Instagram</a>
                    <a class="text-warning text-decoration-none ms-2" href="#" aria-label="twitter">Twitter</a>
                </small>
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
